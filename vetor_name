#include <stdio.h>
#include <string.h>

int main() {
    char nome[100];
    
    printf("Digite o seu nome completo: ");
    fgets(nome, sizeof(nome), stdin);
    
    
    printf ("Bem-vindo(a), %s", nome);
    
    
    return 0;
}
