#include <stdio.h>

int main() {
    int vet[5];
    int maior, menor;
    
    for (int i = 0; i<5; i++){
        printf("Digite um valor: ");
        scanf("%d", &vet[i]);
    }
maior = vet[0];
menor = vet[0];

for(int i = 0; i<5; i++){
    if(vet[i] > maior){
        maior = vet[i];
    }
    if(vet[i] < maior){
        maior = vet[i];
    }
}
printf("O maior número da lista é %d\n", maior); 
printf("O maior número da lista é %d\n", menor); 

}
