#include <stdio.h>
int main() {
    
    int n = 5;
    int soma = 0;
    
    for(int i = 1; i <= n; i++){
        soma = soma + i;
    printf("Passo %d: Soma parcial = %d\n", i, soma);
        }
    printf("==================================\n");
    printf("O valor final é de %d", soma);
}
